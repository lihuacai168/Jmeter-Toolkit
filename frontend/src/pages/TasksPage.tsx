import React from 'react';
import TaskList from '../components/TaskList';

const TasksPage = () => {
    return (
        <div>
            <TaskList />
        </div>
    );
};

export default TasksPage;
